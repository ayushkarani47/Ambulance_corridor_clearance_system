/**
 * RoutePolyline — placeholder component.
 * 
 * Route rendering is currently handled directly in MapContainer
 * using the google.maps.Polyline class for more control.
 * 
 * This file exists as a future upgrade point if @vis.gl/react-google-maps
 * adds a native Polyline component.
 */

export default function RoutePolyline() {
  return null;
}
